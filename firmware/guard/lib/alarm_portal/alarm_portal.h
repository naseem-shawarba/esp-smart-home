#pragma once

// Opens the shared web portal configured for the alarm node (device name/id, mesh fields,
// the alarm settings page, LED blink, and the portal button as the exit).
namespace alarm_portal
{
  void open();
}
